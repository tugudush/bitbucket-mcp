# Bitbucket MCP Server Environment Configuration

# Copy this file to .env and fill in your credentials
# DO NOT commit the .env file to version control

# Bitbucket Authentication (optional but recommended for private repos)
# Choose ONE authentication method:

# Method 1: API Tokens (RECOMMENDED - replaces App Passwords as of Sept 2025)
BITBUCKET_API_TOKEN=your-api-token

# Method 2: App Passwords (LEGACY - being deprecated Sept 9, 2025)
BITBUCKET_USERNAME=your-bitbucket-username
BITBUCKET_APP_PASSWORD=your-app-password

# How to create an API Token (RECOMMENDED):
# 1. Go to your Bitbucket account settings
# 2. Navigate to API tokens under Access management
# 3. Create a new API token with these permissions:
#    - Repositories: Read
#    - Pull requests: Read
#    - Issues: Read
#    - Account: Read
# 4. Copy the generated token and use it as BITBUCKET_API_TOKEN

# How to create an App Password (LEGACY - for backward compatibility):
# 1. Go to your Bitbucket account settings
# 2. Navigate to App passwords under Access management
# 3. Create a new app password with these permissions:
#    - Repositories: Read
#    - Pull requests: Read
#    - Issues: Read
#    - Account: Read
# 4. Copy the generated password and use it as BITBUCKET_APP_PASSWORD
